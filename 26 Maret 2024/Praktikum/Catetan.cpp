Constructor adalah fungsi otomatis dan dipanggil setiap dilakukan instansiasi terhadap kelas.
contstructor digunakan untuk alokasi ruang bagi sebuah objek.
sama seperti fungsi, constructor dapat diberi parameter namun tidak memiliki fungsi return;

Destructor kebalikan constructor, berjalan otomatis saat objek ingin dihapus, tidak memiliki tipe data,
berfungsi membersihkan memori dan data  
